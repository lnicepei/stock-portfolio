import styled from "styled-components";

export const StyledUserCoinsModal = styled.div`
  position: absolute;
  top: 10vh;
  right: 0;
  box-sizing: border-box;
  max-height: 50vh;
  width: max(20vw, 200px);
  overflow-y: scroll;
  background-color: black;
  border-radius: 0 0 1rem 1rem;
  z-index: 2;
`;
